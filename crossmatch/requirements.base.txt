antares-client[subscriptions]
astropy
django>=6.0,<6.1
django_celery_results
django-celery-beat
django-tables2
flower
# WSGI server for the read-model HTTP API web workload (entrypoints/run_web.sh).
# Server-only: not in the Dask (de)serialization path, so no cluster-version
# alignment is required (unlike numpy/pandas/dask). Pinned per the
# dependency-pin convention (docs/solutions/conventions/).
gunicorn==23.0.0
# Serves the web frontend's collected static assets (logo, css) directly from
# the gunicorn pod, no nginx. Server-only: not in the Dask (de)serialization
# path, so no cluster-version alignment is required. Pinned per the
# dependency-pin convention; requirements.lock is regenerated in the same commit.
whitenoise==6.12.0
hop-client
httpx
confluent_kafka
lasair
lsdb==0.10.4
# hats and nested-pandas move with lsdb and are pinned explicitly: lsdb 0.10.4
# requires hats>=0.10.4,<0.11 and nested-pandas>=0.6.10,<0.7.0. Both sit above
# the Dask serialization boundary (not in core/dask.py's _VERSION_CHECK_PACKAGES),
# so an app/cluster skew is caught by the lsdb/hats runtime guard in
# core/dask.py; the end-to-end DEV smoke is the load-bearing verification.
hats==0.10.4
nested-pandas==0.6.10
pittgoogle-client
numpy==2.4.2
pandas==2.3.3
tornado==6.5.4
dask[complete]==2026.1.2
s3fs
prometheus-client==0.25.0
psycopg[binary]
redis
structlog
valkey
watchdog
